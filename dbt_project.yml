name: 'centec_analytics'
version: '1.0.0'
config-version: 2

profile: 'iso_compliance_dw'

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]

models:
  centec_analytics:
    staging:
      +materialized: view
      +schema: staging
    intermediate:
      +schema: intermediate
      +materialized: view
    marts:
      +materialized: table
      +schema: marts